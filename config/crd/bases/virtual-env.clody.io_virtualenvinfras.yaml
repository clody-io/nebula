---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.16.1
  name: virtualenvinfras.virtual-env.clody.io
spec:
  group: virtual-env.clody.io
  names:
    kind: VirtualEnvInfra
    listKind: VirtualEnvInfraList
    plural: virtualenvinfras
    singular: virtualenvinfra
  scope: Namespaced
  versions:
  - name: v1
    schema:
      openAPIV3Schema:
        description: VirtualEnvInfra is the Schema for the virtualenvinfras API
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: VirtualEnvInfraSpec defines the desired state of VirtualEnvInfra
            properties:
              provider:
                description: Foo is an example field of VirtualEnvInfra. Edit virtualenvinfra_types.go
                  to remove/update
                type: string
            required:
            - provider
            type: object
          status:
            description: VirtualEnvInfraStatus defines the observed state of VirtualEnvInfra
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
