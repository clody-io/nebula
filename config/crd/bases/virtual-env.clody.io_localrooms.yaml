---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.13.0
  name: localrooms.virtual-env.clody.io
spec:
  group: virtual-env.clody.io
  names:
    kind: LocalRoom
    listKind: LocalRoomList
    plural: localrooms
    singular: localroom
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .spec.image
      name: Image
      type: string
    - jsonPath: .spec.cpu
      name: CPU
      type: string
    - jsonPath: .spec.memory
      name: Memory
      type: string
    - jsonPath: .status.phase
      name: Phase
      type: string
    - jsonPath: .metadata.creationTimestamp
      name: Age
      type: date
    name: v1
    schema:
      openAPIV3Schema:
        description: LocalRoom is the Schema for the localrooms API
        properties:
          apiVersion:
            description: 'APIVersion defines the versioned schema of this representation
              of an object. Servers should convert recognized schemas to the latest
              internal value, and may reject unrecognized values. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources'
            type: string
          kind:
            description: 'Kind is a string value representing the REST resource this
              object represents. Servers may infer this from the endpoint the client
              submits requests to. Cannot be updated. In CamelCase. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds'
            type: string
          metadata:
            type: object
          spec:
            description: LocalRoomSpec defines the desired state of LocalRoom
            properties:
              containerName:
                description: Foo is an example field of LocalRoom. Edit localroom_types.go
                  to remove/update Foo string `json:"foo,omitempty"`
                type: string
              cpu:
                type: string
              image:
                type: string
              memory:
                type: string
            required:
            - containerName
            - cpu
            - image
            - memory
            type: object
          status:
            description: LocalRoomStatus defines the observed state of LocalRoom
            properties:
              lastUpdated:
                format: date-time
                type: string
              message:
                type: string
              phase:
                description: 'INSERT ADDITIONAL STATUS FIELD - define observed state
                  of cluster Important: Run "make" to regenerate code after modifying
                  this file'
                type: string
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
