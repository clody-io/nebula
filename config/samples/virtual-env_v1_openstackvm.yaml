apiVersion: virtual-env.clody.io/v1
kind: OpenstackVM
metadata:
  labels:
    app.kubernetes.io/name: nebula
    app.kubernetes.io/managed-by: kustomize
  name: openstackvm-sample
spec:
  # TODO(user): Add fields here
